{
  "name": "wahlkompass",
  "version": "0.1.0",
  "private": true,
  "description": "Open-Source-Wahlkompass — live, alle Parteien, vollständig offen",
  "license": "AGPL-3.0",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "cd apps/web && npm run dev",
    "dev:api": "cd apps/api && uvicorn src.main:app --reload --port 8000",
    "test": "echo 'Run tests per package: packages/*/tests/' && exit 0"
  },
  "engines": {
    "node": ">=20"
  }
}
